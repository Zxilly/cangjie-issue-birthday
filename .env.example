# GitCode OAuth —— client_id 是公开值（出现在授权 URL 里），可写死在 src/config.ts 或填这里。
VITE_GITCODE_CLIENT_ID=

# 说明：token 交换/刷新经同域的 Pages Function /oauth/token 代理；
# client_secret 不在前端，而是设到 Cloudflare Pages 项目的 Secret（GITCODE_CLIENT_SECRET）。
# 本地 `wrangler pages dev` 用根目录 .dev.vars（见 .dev.vars.example）。
